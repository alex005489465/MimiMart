# ============================================================
# AWS Security Groups 模組 - 變數範例檔
# ============================================================
#
# 使用方式:
# 1. 複製此檔案: cp terraform.tfvars.example terraform.tfvars
# 2. 編輯 terraform.tfvars,填入實際的 VPC ID
# 3. 執行 Terraform 指令
#
# 注意事項:
# - terraform.tfvars 不會上傳到 Git (已在 .gitignore 中)
# - AWS 認證資訊從 aws/.env 檔案透過環境變數載入,無需在此設定
# ============================================================

# ============================================================
# AWS 認證資訊
# ============================================================
# 這些變數從 aws/.env 檔案透過 TF_VAR_* 環境變數載入
# 請勿在此檔案中填寫實際的認證資訊
#
# aws_access_key = "[從 aws/.env 載入]"
# aws_secret_key = "[從 aws/.env 載入]"
# aws_region     = "[your-aws-region]"

# ============================================================
# 專案基本資訊
# ============================================================

project_name = "your-project-name"
environment  = "prod"

# ============================================================
# VPC 配置
# ============================================================
# 從 VPC 模組輸出取得 VPC ID
#
# 查詢 VPC ID 指令:
# docker-compose --env-file aws/.env run --rm terraform "cd vpc && terraform output vpc_id"
#
# 範例輸出: vpc-0a1b2c3d4e5f6g7h8

vpc_id = "vpc-xxxxxxxxxxxxxxxxx"  # 替換為實際的 VPC ID

# ============================================================
# 標籤配置 (可選)
# ============================================================
# 如需添加額外的自訂標籤,可以在此設定

additional_tags = {
  # CostCenter = "Engineering"
  # Owner      = "DevOps"
  # Backup     = "Daily"
}

# ============================================================
# IP 範圍配置說明
# ============================================================
#
# 📌 管理員 IP 和 Cloudflare IP 由 modules/ip-ranges 模組管理
#
# SSH Security Group 使用:
# - module.ip_ranges.admin_only_v4 (管理員 IPv4)
# - module.ip_ranges.admin_only_v6 (管理員 IPv6)
#
# Web Security Group 使用 (目前註解):
# - module.ip_ranges.cloudflare_ipv4 (15 個 IPv4 CIDR)
# - module.ip_ranges.cloudflare_ipv6 (7 個 IPv6 CIDR)
#
# 如何更新管理員 IP:
# 1. 編輯 modules/ip-ranges/developer-ips.json
# 2. 重新執行 terraform apply
#
# 查詢當前 IP 指令:
# - IPv4: curl -4 ifconfig.me
# - IPv6: curl -6 ifconfig.me
# ============================================================
