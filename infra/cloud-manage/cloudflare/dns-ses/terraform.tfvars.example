# ====================================================================
# Terraform 變數配置範例 - SES DNS 記錄自動化
# ====================================================================
# 使用說明:
#   1. 複製此檔案為 terraform.tfvars
#   2. 將所有佔位符替換為實際值
#   3. terraform.tfvars 已在 .gitignore 中,不會被提交
# ====================================================================

# --------------------------------------------------------------------
# Cloudflare 認證資訊
# 注意: 這些變數應從 cloudflare/.env 檔案透過環境變數載入
#       無需在此檔案中設定
# --------------------------------------------------------------------

# cloudflare_api_token = "[YOUR_CLOUDFLARE_API_TOKEN]"
# cloudflare_zone_id   = "[YOUR_CLOUDFLARE_ZONE_ID]"

# --------------------------------------------------------------------
# 域名配置
# --------------------------------------------------------------------

# 主域名（必須與 SES 模組中的 domain_name 一致）
domain_name = "example.com"

# --------------------------------------------------------------------
# DMARC 配置（可選）
# --------------------------------------------------------------------

# DMARC 政策: none (只監控)、quarantine (隔離可疑郵件)、reject (拒絕可疑郵件)
# 建議初期使用 quarantine,觀察一段時間後再改為 reject
dmarc_policy = "quarantine"

# 接收 DMARC 彙整報告的郵箱（可選,填寫後會收到每日彙整報告）
dmarc_rua_email = ""

# 接收 DMARC 失敗報告的郵箱（可選,填寫後會收到即時失敗通知）
dmarc_ruf_email = ""
