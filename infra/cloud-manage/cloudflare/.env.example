# 平台識別（docker-compose 會使用此變數）
PLATFORM=cloudflare

# Cloudflare API Token（機密）
# 從 https://dash.cloudflare.com/profile/api-tokens 取得
# 建議使用「編輯 Zone DNS」模板
TF_VAR_cloudflare_api_token=your-cloudflare-api-token-here

# Cloudflare Zone ID
# 從 Cloudflare Dashboard 的網域頁面右側欄位取得
TF_VAR_cloudflare_zone_id=your-zone-id-here

# Cloudflare Account ID
# 從 Cloudflare Dashboard 的網域頁面右側欄位取得
TF_VAR_cloudflare_account_id=your-account-id-here

# 網域名稱
# 基礎網域，不含子網域（例如 yourdomain.com）
TF_VAR_domain_name=yourdomain.com
