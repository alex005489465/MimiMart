# 注意：以下變數由 Docker 環境變數提供，不需要在此檔案中設定：
# - cloudflare_api_token (透過 TF_VAR_cloudflare_api_token)
# - cloudflare_account_id (透過 TF_VAR_cloudflare_account_id)
#
# 這些變數在 cloudflare/.env 檔案中配置

# ============================================================================
# Pages 專案配置
# ============================================================================

pages_project_name = "my-project-admin"
production_branch  = "main"

# ============================================================================
# 自訂域名配置
# ============================================================================

custom_domain = "admin.example.com"
